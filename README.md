# 14-Sonli umumta'lim maktabi — rasmiy veb-sayt

Next.js 16 + TypeScript + Tailwind CSS. Uch tilli: o‘zbek (standart), rus, ingliz.

```bash
npm install
npm run dev     # http://localhost:3000
npm run build
npm run lint
```

Loyiha tuzilmasi va qoidalar: [CLAUDE.md](./CLAUDE.md).
